# Copy to .env or .env.local and fill in values. Do not commit .env or .env.local.
# Used by Vite at build time; prefix with VITE_ to expose to client.

# Backend API base URL (e.g. http://localhost:8000 for local dev)
VITE_API_URL=http://localhost:8000
